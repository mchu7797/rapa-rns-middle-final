---
# running-config -> startup-config 저장 (write memory)
- name: 구성 저장
  hosts: routers
  gather_facts: false
  tasks:
    - name: startup-config 로 저장
      cisco.ios.ios_config:
        save_when: always
